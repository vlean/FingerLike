name: Build macOS

on:
  push:
    branches: [ main, develop ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.6'
          channel: 'stable'
          cache: true

      - name: Flutter version
        run: flutter --version

      - name: Get dependencies
        run: flutter pub get

      - name: Analyze code
        run: flutter analyze --no-fatal-warnings --no-fatal-infos

      - name: Build macOS app
        run: flutter build macos --release

      - name: Get version
        id: version
        run: |
          if [[ "${{ github.ref }}" =~ refs/tags/v(.*) ]]; then
            echo "VERSION=${BASH_REMATCH[1]}" >> $GITHUB_OUTPUT
          else
            echo "VERSION=latest" >> $GITHUB_OUTPUT
          fi

      - name: Create DMG
        run: |
          version="${{ steps.version.outputs.VERSION }}"
          dist_dir="dist"
          mkdir -p "$dist_dir"

          app_path="build/macos/Build/Products/Release/FingerLike.app"

          if [ -d "$app_path" ]; then
            # Create a simple ZIP archive (DMG requires additional tools)
            cd "$app_path/.."
            zip -r "../../../$dist_dir/FingerLike-macOS-$version.zip" "FingerLike.app"
            cd "../../../"
          else
            echo "Error: FingerLike.app not found at $app_path"
            exit 1
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: FingerLike-macOS-${{ steps.version.outputs.VERSION }}
          path: dist/*.zip
          retention-days: 30

      - name: Create Release (if tagged)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: dist/*.zip
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
